package luna.lunasorigins.origin;

import net.minecraft.util.Identifier;

import java.util.HashMap;
import java.util.Map;

public class CustomOriginScreens {
    /*  private static final Identifier DEFAULT_TEXTURE = new Identifier("lunasorigins", "textures/gui/choose_origin.png");
    private static final Map<String, Identifier> customBackground = new HashMap<>();
    
    public static void register(String originId, Identifier texture) {
        customBackground.put(originId, texture);
    }
    
    public static Identifier get(String originId) {
        return customBackground.getOrDefault(originId, DEFAULT_TEXTURE);
    
    }*/

}
